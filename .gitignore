.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
transcripts/
transcription_state.json
transcription_state.json.corrupt
tertius.log

# transcripts and uploaded media never belong in the repo
_uploads/
*.srt
*.txt
!requirements.txt

# the design bundle is kept unpacked; the archive is a duplicate
app/design/*.zip

# Written by the launcher on every run. All three hold absolute paths to this
# folder on this machine, so they are never the same on two computers.
/Tertius.lnk
/desktop.ini
/Tertius.desktop
